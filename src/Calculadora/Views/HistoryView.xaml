<UserControl x:Class="Calculadora.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1C1C1C" Foreground="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabecera -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Historial de Operaciones" FontSize="24" FontWeight="SemiBold"/>
            <Button Grid.Column="1" Content="🗑 Limpiar" 
                    Command="{Binding ClearHistoryCommand}"
                    Background="#48484A" Foreground="#AEAEB2" BorderThickness="0"
                    Padding="15,8" FontSize="14">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <!-- Lista de entradas -->
        <ListBox Grid.Row="1" ItemsSource="{Binding Entries}" 
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,6"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Focusable" Value="False"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="#2C2C2E" CornerRadius="8" Padding="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Módulo y hora -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,4">
                                <Border Background="#0A84FF" CornerRadius="3" Padding="6,2" Margin="0,0,8,0">
                                    <TextBlock Text="{Binding Module}" FontSize="11" Foreground="White"/>
                                </Border>
                                <TextBlock Text="{Binding FormattedTime}" FontSize="11" Foreground="#8E8E93" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Botón copiar -->
                            <Button Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Content="📋" 
                                    Command="{Binding DataContext.CopyExpressionCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    Background="Transparent" Foreground="#8E8E93" BorderThickness="0"
                                    FontSize="16" VerticalAlignment="Center" Padding="8,4"
                                    ToolTip="Copiar expresión"/>

                            <!-- Expresión = Resultado -->
                            <TextBlock Grid.Row="1" Grid.Column="0" FontSize="15" FontFamily="Consolas" TextWrapping="Wrap">
                                <Run Text="{Binding Expression, Mode=OneWay}" Foreground="White"/>
                                <Run Text=" = " Foreground="#8E8E93"/>
                                <Run Text="{Binding Result, Mode=OneWay}" Foreground="#30D158"/>
                            </TextBlock>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
